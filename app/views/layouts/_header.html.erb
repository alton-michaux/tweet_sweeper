<div class='container'>
	<div class='row'>
		<div class='col-12'>
				<div class='float-end'>
					<%# <%= link_to 'Profiles', user_profiles_path(user_id: @user.id), class: 'btn btn-primary' %>
					<%# <%= link_to 'Tweets', user_tweets_path(user_id: @user.id), class: 'btn btn-primary' %> 
				</div>
				<p><%= link_to 'TweetSweeper', root_path, class: 'navbar-brand text-light text-decoration-none' %></p>
		</div>
	</div>
</div>